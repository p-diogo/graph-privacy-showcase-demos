{
  "name": "private-bond-anchors",
  "version": "0.1.0",
  "private": true,
  "description": "Anchor surface of EthSystems' private-bond PoC, served for auditor replay",
  "scripts": {
    "prepare:local": "./scripts/prepare-local.sh",
    "codegen": "npm run prepare:local && graph codegen subgraph.local.yaml",
    "build": "npm run codegen && graph build subgraph.local.yaml",
    "test": "npm run codegen && graph test",
    "create:local": "graph create --node http://127.0.0.1:8020 private-bond-anchors",
    "deploy:local": "graph deploy --node http://127.0.0.1:8020 --ipfs http://127.0.0.1:5001 --version-label v0.1.0 private-bond-anchors subgraph.local.yaml"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.1",
    "matchstick-as": "0.6.0",
    "mustache": "4.2.0"
  }
}

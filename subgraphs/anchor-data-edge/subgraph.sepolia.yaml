# Generated into subgraph.yaml by `pnpm manifest` — do not edit the generated
# file. Templating follows the EBO precedent (block-oracle's mustache manifest);
# the placeholders are the values that must be pinned per deployment.
#
# specVersion 1.3.0 is docs-latest; call handlers need nothing newer, and EBO
# ran them on 0.0.5. If an arranged indexer's graph-node predates 1.3.0 the
# floor drops with no design change.
#
# `mapping.kind: ethereum/events` is correct for call handlers — the EBO
# manifest does exactly this. Call handlers are a handler kind, not a feature,
# so no `features:` block is required. They do require trace-capable chain
# data: graph-node asks for `traces` whenever a data source has a call handler
# and fetches calls with the Parity-style `trace_filter` RPC.
specVersion: 1.3.0
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: AnchorDataEdge
    network: sepolia
    source:
      address: "0x88AE18d29C20267441C5393787f719fa3f334Dcb"
      abi: AnchorDataEdge
      startBlock: 11376118
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      entities:
        - Stream
        - Anchor
        - ConflictingAnchor
        - MalformedAnchor
      abis:
        - name: AnchorDataEdge
          file: ./abis/AnchorDataEdge.json
      callHandlers:
        - function: postAnchor(bytes)
          handler: handlePostAnchor
      file: ./src/mapping.ts
